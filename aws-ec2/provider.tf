# To Add : avoid hardcoding the login keys and credential keys file paths , make a prompts which appears where we can enter the keys  

# provider "aws" {
#   access_key = var.AWS_ACCESS_KEY
#   secret_key = var.AWS_SECRET_KEY
#   region     = var.AWS_REGION
# }

